// Joining Array
// Return as string
const number1 = [1, 2, 3, 4];
const joinedNumber = number1.join(',');
console.log(joinedNumber)

// Splitting String
// Return an array
const text = "Welcome to Javascript World";
const splitArray = text.split(" ");
console.log(splitArray);